const _ = require('../src/collection.js');
const expect = require('chai').expect;

describe('Collection countBy', () => {
    // it('should throw type error', () => {
    //     let fn = () => _.countBy(1);
    //     expect(fn).to.throw(TypeError);
    // });
    // it('should countBy arr 1', () => {
    //     expect(_.countBy([6.1, 4.2, 6.3], Math.floor)).to.deep.equal({ '4': 1, '6': 2 });
    //     expect(_.countBy(['one', 'two', 'three'], 'length')).to.deep.equal({ '3': 2, '5': 1 });
    // });
});         